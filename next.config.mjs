/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
   images: {
    domains: ["myacernity.vercel.app"],
  },
};

export default nextConfig;
